---
framework: jest
preRun:
     command:
        - npm install
preMerge:
  pattern:
    - "./test/*/*.js"
    - "./test/*.js"
postMerge:
  pattern: 
    - "./test/utils/*.js"
    - "./test/*.js"
postRun:
     command:
        - npm --version
version: 1.0
